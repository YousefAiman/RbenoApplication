<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:title="المزيد"
        app:showAsAction="ifRoom"
        android:icon="@drawable/dots_icon">
        <menu>
            <item
                android:id="@+id/edit_account"
                android:title="تعديل الملف"
                android:icon="@drawable/edit_account_icon"
                app:showAsAction="ifRoom" />
            <item
                android:id="@+id/status_present"
                android:title="تغيير الحالة"
                android:icon="@drawable/green_circle"
                app:showAsAction="ifRoom" />
            <item
                android:id="@+id/blocked_users"
                android:title="المستخدمين المحظورين"
                app:showAsAction="ifRoom" />
        </menu>
    </item>

</menu>